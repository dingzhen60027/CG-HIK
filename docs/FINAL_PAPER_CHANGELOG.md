# Final paper changelog: task-contract alignment

Date: 2026-09-08. Branch: `codex/hierarchical-v5`.
Incoming research baseline: `02aa287b65d4f1b7bb5317d8e83fcf972559103f`.
Frozen stage-one delivery: `089a3fe7ec0456002aa0f3b9661119b99072fad7`,
pushed and confirmed remotely before any stage-two manuscript edits.

## Stage one: fixed measurement plan completed

- Added the independent `task_contract_alignment` module, config, launcher,
  protocol, fixed input identities, native mappings, smoke tests and unit tests.
- Reused the frozen witnessed/inexecutable point inputs; completed both robots'
  point arms and fixed contract-scale sensitivity.
- Generated and ran the prespecified symmetric UR5e trajectories. Read the existing
  Panda trajectory records as authoritative evidence, without rerunning them.
- Kept internal status, task verification and deadline-qualified verification
  distinct. Saved raw returns, phase timings, DLS traces, full-trajectory witnesses,
  completion UID sets, families and paired uncertainty.
- Completed independent replay and preservation checks. No model, threshold,
  public verifier, historical trajectory or old raw record changed.
- A duplicate `robot` keyword stopped aggregation after measurement. The disclosed
  analysis-only correction merged that dictionary correctly; it did not change
  measurements or rerun solver calls. Original execution seal and the separate
  `analysis_revision.json` remain intact.
- Generated three main tables, five main figures, the DLS supplement and the
  five-question findings report. No global gate and no result-driven tuning.

## Stage two: full reconstruction, no experiments

The new title is **Task-Contract-Aware Online Inverse Kinematics: Aligning Solver
Convergence with Command Admissibility**.

- Replaced the routing-first abstract, Introduction, method, protocol, results,
  discussion and conclusion, rather than changing only the title.
- Organized the Introduction in seven paragraphs and the Results around three
  questions. Prior analytical, numerical, constrained and set-based IK are treated
  as foundations, not deficient competitors.
- Added the three-outcome formulation, exact-input witness semantics, conservative
  component-box mapping and dynamic rate intervals.
- Retained the strict TRAC witnessed-point success ceiling and the adverse UR5e
  DLS trajectory result. The paper does not force a universal alignment benefit.
- Kept CG-HIK as a frozen secondary allocation case study. No development version
  sequence, training claims or universal successful-query acceleration remains.
- Concentrated physical, timing, numerical and statistical boundaries in the
  Discussion and Limitations.
- Rebuilt the evidence snapshot, generated numbers/table rows and active source
  CSVs. The figure builder copies hash-verified frozen PDF/SVG/PNG exports.
- Rebuilt the PDF, checked every page and validated its citations and generated
  numeric keys.
- Retained 38 references, including 22 dated 2024–2026. Exact metadata, primary
  sources, preprint/conference status and unresolved optional fields are recorded
  in `REFERENCE_VERIFICATION_TASK_CONTRACT.md`; no guessed CppFlow page range.

## Historical preservation

- The complete incoming paper is retained in
  `paper/history/cghik_paper_02aa287.tar.gz`, including its PDF and source data.
- The preceding claim map, changelog and research record are retained in
  `docs/history/task_contract_predecessor_02aa287.tar.gz`.
- Unused older figures/derived CSVs remain available but are not inputs to the new
  manuscript. Frozen outputs, models, runtimes, configs and manifests are untouched.
- The unrelated pre-existing untracked `docs/STORY_FLOW_AUDIT.md` is not part of
  this delivery.

## Stop point and submission preparation

The scientific work is complete; no further algorithm or experiment is scheduled.
The generic journal manuscript still needs the real author/affiliation details,
CRediT roles, funding/conflict declarations and the selected journal's final
submission formatting. No identities, funding sources or repository DOI have been
invented. Machines and Applied Sciences are scope-compatible engineering targets;
this is not an acceptance prediction or a claim of completed journal submission.
