protocol_version: 4
experiment_name: counterfactual_v4_current_code_readiness_smoke
workspace: ..
source_config: paper_v2.yaml
release_root: ../outputs/release_v3_locked
output_root: ../outputs/counterfactual_v4_readiness_smoke_r2

robots: [panda, ur5e]
training_seeds: [17]

data:
  source_role: risk_validation_queries
  pilot_count: 16
  dt: 0.02

timing:
  warmup_iterations: 8
  repeats: 4
  deadline_ms: 20.0

runtime:
  device: cuda:0
  intra_op_threads: 8
  inter_op_threads: 1
  max_unrelated_cpu_percent: 50.0
  environment_check_every_queries: 1
  quiet_stable_checks: 2
  quiet_poll_seconds: 1.0
  max_quiet_wait_seconds: 600.0

logging:
  progress_every: 4
