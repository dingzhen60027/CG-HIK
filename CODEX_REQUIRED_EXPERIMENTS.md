# 单一有界 GN：必要实验一次性执行任务

## 0. 任务与边界

仓库：`dingzhen60027/CG-HIK`  
分支：`codex/hierarchical-v5`  
已核实基线：`b34800d4e38f391ad873b0a95c152e7da4a1081f`

本任务取代上一份任务书中的“自动写论文、制作 cover letter、投稿格式化”要求。
本轮完成必要实验及贡献判断，不改论文、不增加新算法、不建立兜底链。
不要在每个子步骤完成后等待下一份提示词：完成适配、局部检查、两机器人比较和结果报告。

主方法固定为现有 `BoundedGN`、κ=1。κ=0 是已有消融，不重新选 κ。
继续当前 NumPy 数值路径及原服务器环境，不修复/启用 Numba，不升级依赖。
旧稿、旧结果、旧模型及基线提交保留；新结果不得覆盖旧目录。
如果远端已出现后续相关实验，先检查并复用有效完成项，不重置分支或重复执行。
不执行 force-push，不混入无关工作树修改。

## 1. 唯一研究问题

在当前目标、上一实际接受状态和单帧关节位移范围已知的连续 IK 中，
将边界直接纳入局部增量求解，能否以可接受的计算成本改善完整跟踪？
关节居中项增加了多少效果？小规模专用 QP 内核相比合理使用的通用内核是否真的有优势？

不假定“所有传统方法都只是裁剪”；TRAC-IK 和 Pink 已有约束处理。
不把活动集、Gauss–Newton、KKT 或关节居中命名为首次发明。
实验区分：数学机制、数值内核效率、完整任务收益。三者不能互相替代。

## 2. 先读取，不做全仓重复审计

- `docs/SINGLE_SOLVER_DEVELOPMENT_FINDINGS.md`
- `src/confik/bounded_gn.py`
- `src/confik/bounded_gn_adapter.py`
- `scripts/run_single_solver_development.py`
- `configs/single_solver_development.yaml`
- `src/confik/correction_reserve/study.py`
- `src/confik/correction_reserve/reporting.py`
- `src/confik/correction_reserve/pink_adapter.py`
- `src/confik/revision_compute_allocation/data.py`
- `src/confik/task_contract_alignment/outcomes.py`
- 原 verifier、当前 development 输入与最近已观察的独立评估输入身份。

把当前 40/40 结果明确记为已观察开发结果，而非独立验证。
附件/项目中较早的 CG-HIK PDF 不是本轮算法说明或结果来源。

## 3. 冻结主方法和共同合同

保持现有主方法逐项设置：
- initial damping=0.01；κ=1；task_stop=1；
- maximum outer updates=30；box-QP active-set updates cap=50；
- 原八级回溯、阻尼调整、末次更新处理及20 ms内部软时限；
- 原位置/姿态残差、SO(3)导数、任务尺度和关节步长尺度；
- 原真实 FK 与独立 verifier。

同核定义：S=diag(velocity*dt+velocity_tolerance)，L=diag(upper-lower)，q_c=(lower+upper)/2。
局部模型为：

0.5*||e(q)+J_r(q)S d||^2
+0.5*lambda*||d||^2
+0.5*kappa*||L^-1(q+S d-q_c)||^2。

保持代码中 H=G.T G+lambda I+kappa diag((step/span)^2)，不要写成 lambda^2。
整个查询的关节区间固定于实际 previous_q：
[max(q_min,previous_q-step), min(q_max,previous_q+step)]。
各次迭代相对于该固定区间形成 d 的上下界；不得把区间中心随内部 q 移动。
保留原有数值向内构造，不放宽验收。

特别注意：现有方向模型含居中项，但回溯只检查非线性任务残差下降。
不要改成另一个 merit function，不要声称代码在单调降低完整姿态正则目标。

公共主合同不变：1 mm、0.5°、dt=20 ms、原关节范围和速度容差。
不加动力学、碰撞、加速度硬约束或真实未来信息。

## 4. 固定七个设置；同核对照不是新研究分支

M1：现有有界 GN，κ=1，原活动集 QP。唯一主方法。
M2：现有有界 GN，κ=0。只去掉居中项。
M3：同核 κ=1，无约束求步后裁剪。
M4：同核 κ=1，以当前已安装 OSQP 求完全相同的局部箱约束 QP。
M5：task-aligned TRAC-IK 5 ms。
M6：task-aligned TRAC-IK 20 ms。
M7：当前固定版本 Pink。

M3 必须严格使用 M1 相同的 H、g、lo、hi：
d_unconstrained=solve(H,-g)，d=clip(d_unconstrained,lo,hi)。
其后沿用同一回溯、阻尼、任务停止和验收。不得额外削弱它。
M3 不代表全部传统方法，只隔离“联合求解边界/事后裁剪”的区别。

M4 使用 P=H、linear term=g、A=I、bounds=(lo,hi)，不得修改H、g、正则或残差。
OSQP 复用固定维度和完整上三角 CSC 模式，通过 update 更新数值。
保留稀疏结构中的零元素，不在每次调用重复初始化库/解析模型。
允许其原生 warm-start；每条轨迹开始按预定规则重置，不能跨轨迹继承有利状态。
数值向量/矩阵转换、更新、求解和必要结果检查都进入计时。

主方法文件不改。用轻量数值回调或独立模块实例实现 M3/M4；默认 M1 路径不变。
不能让同进程的全局 monkey-patch 污染另一种方法。
证明 M1 默认输出路径没有因插入实验接口而改变即可，不开展全仓等价大审计。

M4不是“新算法”；它是为了分离专用QP实现与外层迭代的作用。
不在本轮开发SNS/LoIK/RangedIK适配器；在差异说明中如实比较其已有能力。
不拿适配不充分的 RangedIK 零完成记录证明性能。

## 5. 实验一：相同 QP 的质量—时间比较

目的：测量专用内核是否在相同数学问题及可比精度下更有效。

### 5.1 取真实子问题

从原开发集每类按原顺序取前两条，共8条/机器人。
运行一次 M1 的“只供采集、不报告在线时延”的 instrumentation，保存实际生成的：
H、g、lo、hi、维度、robot、trajectory UID、frame、outer iteration。

不把采集字典、特征分析和磁盘写入混入正式主方法计时。
按UID均衡和固定种子抽取最多1000个QP/机器人；不足就全保留。
抽样不得依赖哪个QP上专用内核更快。
记录完整来源分布，再描述性区分无活动边界/有活动边界、近奇异/普通等类别。
区分“物理关节限位”和“当前帧位移限位”，不要全写成关节限位。

### 5.2 比较方法

同一H、g、lo、hi分别交给：
- 当前 NumPy 活动集内核；
- 缓存结构、重置迭代初值的 OSQP；
- 缓存结构、按记录顺序 warm-start 的 OSQP；
- 无约束解后裁剪，仅用作机制诊断，不当作同精度求解器。

OSQP 的 accuracy 设置仅在该开发QP库上依据数值质量选定，不依据完整轨迹成功率。
使用统一、单独复算的 feasibility / projected-KKT / objective 检查。
可从 eps_abs=1e-8、eps_rel=0、polishing=True、max_iter=20000 开始；
若精度不足，只允许收紧数值容差，记录设置，然后在独立轨迹运行前固定。
不要为了制造速度优势，给OSQP设置远超实际对照需要的精度。
也不要为追求低时延接受精度明显更差的解。

### 5.3 必须报告

- 原始与按问题尺度归一化的KKT残差；
- 箱约束违反量，活动集迭代达到上限的次数；
- 两种合格求解结果的目标差；
- 初始化/转换、矩阵更新与求解时间；
- 整个子问题调用的P50/P95/P99及配对时间比；
- 全部输入及数值不合格输入，不丢弃慢例/坏例。

setup只报告一次成本及其口径；主要稳态比值不得只拿“重新setup的OSQP”做分母。
全局不利结果也保留。相同目标的正确解应相近；不期待专用内核凭相同QP创造更好解。
微基准只能支撑该内核效率，不能直接支撑整轨迹优势或全局复杂度改进。

## 6. 实验二：同核机制（与独立评估共用运行，不重复大规模开发比较）

先用开发集每类前两条做 M1–M4 的一次轻量集成，检查接口与验收即可。
允许修正 ablation adapter 的实现错误；不要重新调主方法。

正式机制结论直接使用下一节独立轨迹中的 M1/M2/M3/M4：
- M1 vs M3：边界内重新分配是否有用？
- M1 vs M2：居中项的独立作用是否只是开发集一例？
- M1 vs M4：同一局部QP的数值实现是否改变实际计算，而不是换了问题？

解释指标：外层更新数、FK/Jacobian调用数、触界次数、约束来源、
回溯次数、原本受限关节之外的运动调整、首次失败和恢复/损失UID。
不同求解器的“内部迭代”不可直接作为同单位成本比较。

需要导数或迭代轨迹图时，用运行中已有日志，或对预先固定开发输入做少量
明确标为诊断的重放；不得将 instrumentation 时间替代生产时延。
不在独立结果出现后挑选新类别、修改算法或加额外场景。

## 7. 实验三：两机器人独立完整轨迹

### 7.1 输入

Panda与UR5e各160条、四类各40条，每条150帧。
类别及生成过程沿用当前开发负载：smooth、near_singular、joint_limit_return、high_curvature。
保持原 dt、轨迹幅度规则、速度利用率规则、参考生成与约束检查。
150帧不改成300帧，避免在同一时间偷偷改变运动速度与难度。

只更换未使用的生成种子与UID。
在任何方法新结果出现前生成全部输入并保存身份、配置、参考可行性检查。
核对已观察开发/正式实验的种子与输入身份；不做全outputs文件哈希大扫描。
若相撞，按预定递增种子规则处理并记录，不基于方法结果换样本。

q_ref只用于离线验证输入，以及共同初始状态。
在线接口仅传当前目标、实际上一接受配置、dt；不传未来目标或q_ref。
不要因某方法完成率不够而放慢轨迹、增加容差或剔除样本。

### 7.2 运行

七个设置都在两机器人全部输入上从第0帧运行，三次整轨迹重复。
统计上仍是每机器人160个独立UID，不是480条独立轨迹或72000个独立帧。
确定性方法重复主要反映计时波动；不声称具有随机种子敏感性。
全部计划共6720次完整轨迹运行、1,008,000帧。

原反馈语义不变：公共verifier接受后更新自己的状态；失败保持；目标索引继续前进。
晚到但合法命令的状态处理沿用原协议；几何完成与按时完成分别统计。
所有目标都进入总成本，不能失败后停止计时或跳帧。

各方法同机、相同线程/CPU资源规则、相同热身流程，按轨迹交错安排方法。
不在计时核心并行运行多个方法争抢核心，不比较历史不同机器时间。
保留全部超时；20ms是软件评价deadline，不是硬实时承诺。

### 7.3 指标

共同主指标：
1. TSR：全部150帧均返回合法命令的轨迹比例。
2. DTSR20：全部150帧均合法且各自总外层调用<=20ms的轨迹比例。

辅助：
- 全部帧P50/P95/P99、最大值、每UID累计时间；
- 同时成功UID上的成本作为描述性补充，不替代全样本；
- 位置/姿态误差P95与最大值、单帧步长；
- 实际命令的加速度RMS，标为运动学有限差分而非实机动力学；
- 首次失败帧及原因、接受命令违约；
- stable三遍改善/损失与平均改善/损失UID，分开报告。

主要配对比较固定为 M1 对 M2、M3、M4、M5、M6、M7。
先在UID内平均重复，再做原有family分层配对bootstrap95%区间。
报告未做多重校正；区间包含零不称等效，100%观测不称零失效率。
两机器人分别报告，不通过合并平均隐藏相反方向。
全样本和四类均报告，不只挑近奇异类。

不设“所有指标必须提高”的总gate，不追加样本直到显著。

## 8. 方法差异说明（最多两页，不写论文）

与已有约束GN/SQP、SNS、微分QP、LoIK的比较，应依据原论文/官方实现：
- 已有方法已经做了什么；
- 当前具体实现如何组合固定帧边界、尺度、局部QP和任务残差回溯；
- 哪些是成熟成分、哪些结果仅说明实现有效；
- 哪项实验真正支持了新增的、可复用的工程/方法认识。

不要求新写40篇综述，不新增上述方法的适配工程。
不能从“我们赢了裁剪版”推断超越所有约束IK。
不能从“专用内核快”推断提出了新的优化理论。
如果通用QP同核版效果相当，只在实际测得差异范围内表述。

## 9. 文件和顺序

复用现有 `run_single_solver_development.py`、runner与统计函数；
新增一个薄入口 `scripts/run_single_solver_evidence.py`，不要复制一套大型框架。
建议一个配置 `configs/single_solver_evidence.yaml`，一个结果根目录：

outputs/single_solver_evidence/
  protocol/
  qp_benchmark/
  trajectories_panda/
  trajectories_ur5e/
  reports/

必要新增回调和测试使用同一个小模块，不建V8/V9目录、不继承旧TAR/Elastic链。

顺序：
1. 读取当前实现，冻结主方法；
2. 完成M3/M4薄适配、必要单元测试；
3. 开发QP采集/质量/计时比较，固定OSQP质量设置；
4. 小规模接口集成；
5. 固定新输入和全部比较设置，提交实验代码/配置；
6. 一次运行两机器人独立实验；
7. 汇总表、机制结果与方法差异说明；
8. 提交相关代码和结果，push当前分支，核对远端SHA。

若任务中断，按completed job恢复，不能混合变更了数值设置的记录。
只修真实实现错误，保留错误来源与重跑范围；不根据收益换参数。

## 10. 本轮完成标准

交付：
- 同核三项区别：边界处理、居中、QP后端；
- 同输入QP质量—时间表，含可复用QP样本；
- 两机器人七设置独立主表及四类分类表；
- 完整UID结果、主要配对区间、损失与超时；
- 源数据生成的结果图，不要求先美化成投稿图；
- `docs/SINGLE_SOLVER_NECESSARY_EXPERIMENTS.md`；
- 两页以内方法差异与claim–evidence清单；
- 实际提交与远端SHA。

报告只回答：
1. 开发集40/40是否在独立数据上保持有用的完成表现？
2. 联合处理边界相对裁剪的真实贡献是什么？
3. 居中项是否有可重现增量？
4. 专用QP内核相对合理配置的通用后端有什么质量/时间差异？
5. 相比TRAC/Pink，完成—时限—成本折中是否值得？
6. 哪种论文贡献受到支持：已知方法的有效实现、数值机制改进，还是尚不足以主张新增方法？

本轮终点是完成以上证据与判断，不是生成投稿PDF。
不修改paper/main.tex，不生成cover letter，不换标题包装结果。
不因结果不利自动添加算法、展开下一轮研究或更换主线。
